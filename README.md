# jepop
Hostpath packets in Juniper routers have an additional Juniper Ethernet header which is not understood outside of Juniper products. This utility reads packets from a .pcap file and strips the juniper ethernet header if found.
